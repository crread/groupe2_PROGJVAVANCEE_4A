using System.Collections.Generic;
using UnityEngine;

namespace Scrips
{
    public class GameScript : MonoBehaviour
    {
        [SerializeField]
        private List<BoxScript> balises = new List<BoxScript>();
        
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
